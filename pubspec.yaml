name: imdumb
description: "Movie app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.1.0
  imdumb_dependencies:
    git:
      url: https://github.com/dewetbaumann/imdumb_dependencies
      ref: v1.0.0
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.5.0
  build_runner: ^2.7.1

flutter:
  uses-material-design: true